---
import Layout from '../layouts/Layout.astro';

// Helper to determine version label
const getVersionLabel = (version) => {
	if (version.includes('-rc')) return 'Release Candidate';
	if (version.includes('-alpha')) return 'Alpha Release';
	if (version.includes('-beta')) return 'Beta Release';
	return 'Stable';
};

// Fetch the latest version from GitHub at build time
let latestVersion = 'v0.6.0'; // Fallback
let versionLabel = getVersionLabel(latestVersion);

try {
	const response = await fetch('https://api.github.com/repos/fajarhide/omni/releases/latest');
	if (response.ok) {
		const data = await response.json();
		latestVersion = data.tag_name;
		versionLabel = getVersionLabel(latestVersion);
	}
} catch (e) {
	console.error('Failed to fetch latest OMNI version:', e);
}
---

<Layout 
	title="OMNI Context OS | Cut AI Agent Token Costs by 90%"
	description="OMNI is the premier Context Operating System for Autonomous AI Agents. Stop hallucinations, manage token budgets, and cut API costs by 90% with our high-speed semantic filter."
>
	<!-- Ambient Glow Background -->
	<div class="ambient-bg" aria-hidden="true">
		<div class="glow-orb primary"></div>
		<div class="glow-orb secondary"></div>
	</div>

	<main>
		<!-- HERO SECTION -->
		<section class="hero">
			<canvas id="hero-canvas"></canvas>
			<div class="hero-content">
				<div class="badge-pill reveal">
					<span class="badge-dot pulse"></span>
					<span class="badge-version" id="omni-version" data-initial-version={latestVersion}>{latestVersion}</span>
					<span class="badge-divider"></span>
					<span class="badge-label" id="omni-status" data-status={versionLabel.toLowerCase().replace(' ', '-')}>{versionLabel}</span>
				</div>
				
				<h1 class="reveal delay-1">
					Stop Paying for<br/>
					<span class="text-gradient">Noisy Tokens.</span>
				</h1>
				
				<p class="hero-desc reveal delay-2">
					<strong>OMNI is the Context Operating System for Autonomous AI Agents.</strong>
					<br/><br/>
					It acts as a high-performance semantic filter between your terminal and LLMs like Claude Code, Cursor, and Copilot. Intelligently distill noisy logs, cache states, and enforce strict token budgets to ensure your agents execute complex loops flawlessly.
					<br/><br/>
					<em>Cut your AI tokens costs by up to 90%. Start building with pure signal.</em>
				</p>

				<div class="hero-cta reveal delay-3">
					<div class="terminal-install">
						<span class="prompt">$</span>
						<code>brew install fajarhide/tap/omni</code>
						<button class="copy-btn" data-copy="brew install fajarhide/tap/omni"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg></button>
					</div>
					<div class="secondary-installs">
						<span class="text-muted">Or use:</span>
						<button class="text-link" data-copy="curl -fsSL omni.weekndlabs.com/install | bash">Universal Installer</button>
						<span class="text-muted">·</span>
						<button class="text-link" data-copy="irm omni.weekndlabs.com/install.ps1 | iex">Windows</button>
					</div>
				</div>
			</div>
		</section>

		<!-- PROBLEM VS SOLUTION -->
		<section id="problem-solution" class="section">
			<div class="container container-md">
				<header class="section-header reveal">
					<h2 class="section-title">The <span class="text-gradient">Context OS</span> Advantage</h2>
					<p class="section-subtitle">Empower your AI agents to work autonomously for hours without token exhaustion or context amnesia.</p>
				</header>

				<div class="comparison-bento reveal">
					<!-- Pillar 1 -->
					<div class="bento-card card-success">
						<div class="card-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
						<h3>1. Token Efficiency & Massive ROI</h3>
						<p>Every byte in the context window costs money. OMNI acts as a smart sieve, guaranteeing zero tokens are wasted on useless loading bars or dependency logs. <strong>Save $35+ per developer every month.</strong></p>
						<ul class="bullet-list">
							<li><strong>Build Outputs:</strong> Compresses a 3,000 token compile error into just 80 tokens of pure signal.</li>
							<li><strong>Test Runners:</strong> Filters out 4,000 tokens of passing tests to show only exact assertion failures.</li>
							<li><strong>DevOps & Infra:</strong> Transforms thousands of lines of Terraform/Kubectl output into concise status summaries.</li>
						</ul>
					</div>

					<!-- Pillar 2 -->
					<div class="bento-card card-success">
						<div class="card-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></div>
						<h3>2. Zero Hallucinations & High Fidelity</h3>
						<p>High compression is dangerous if the AI loses facts. OMNI's multi-agent architecture ensures your agent never loses its train of thought, even after 4 hours of autonomous coding.</p>
						<ul class="bullet-list">
							<li><strong>Context Pressure Management:</strong> Proactively warns the AI before the context window crashes.</li>
							<li><strong>Critical File Pinning:</strong> Permanently pins <code>AGENTS.md</code> or <code>.cursorrules</code> so core instructions are never forgotten.</li>
							<li><strong>Dependency Graph Indexer:</strong> Warns agents when they mutate high-impact, frequently imported files.</li>
						</ul>
					</div>
					
					<div class="bento-item span-2" style="display: flex; flex-direction: column; align-items: center; justify-content: center; background: transparent; box-shadow: none; border: none;">
						 <img src="/media/performance.png" alt="OMNI Token Savings Strategy" style="max-width: 100%; border-radius: 12px; box-shadow: 0 10px 40px rgba(0,0,0,0.5); border: 1px solid var(--border-subtle);">
					</div>
				</div>
			</div>
		</section>

		<!-- BEFORE / AFTER TERMINAL VISUAL -->
		<section class="section alt-bg">
			<div class="container">
				<header class="section-header reveal">
					<h2 class="section-title">Feel the <span class="text-gradient">Difference</span></h2>
					<p class="section-subtitle">Watch OMNI compress 18,400 tokens of raw logs into 132 tokens of actionable pure signal.</p>
				</header>

				<div class="terminal-showcase reveal">
					<div class="term-window before">
						<div class="term-header">
							<div class="term-dots"><span class="dot red"></span><span class="dot yellow"></span><span class="dot green"></span></div>
							<div class="term-title">Before OMNI (~18,400 Tokens)</div>
						</div>
						<div class="term-body">
							<div class="cli-line"><span class="prompt">$</span> cargo check</div>
							<div class="cli-output dim">		Updating crates.io index
		Downloading crates ...
		Downloaded serde v1.0.210
		Downloaded regex v1.10.6
		... (482 more downloads) ...
	 Compiling proc-macro2 v1.0.86
	 Compiling unicode-ident v1.0.12
	 Compiling serde v1.0.210
warning: unused variable: `idx`
	--> src/parser.rs:42:9
	 |
42 |     let idx = 0;
	 |         ^^^ help: if this is intentional, prefix it with an underscore: `_idx`
	 Compiling ryu v1.0.18
... (2,300 lines of compilation logs) ...
error[E0308]: mismatched types
	--> src/main.rs:12:30
	 |
12 |     let result: String = 100i32;
	 |                          ^^^^^^ expected `String`, found `i32`
error: could not compile `omni` (bin "omni") due to 1 previous error</div>
						</div>
					</div>

					<div class="term-window after">
						<div class="term-header">
							<div class="term-dots"><span class="dot red"></span><span class="dot yellow"></span><span class="dot green"></span></div>
							<div class="term-title">With OMNI Semantic Engine (132 Tokens)</div>
						</div>
						<div class="term-body">
							<div class="cli-line"><span class="prompt">$</span> cargo check | omni</div>
							<div class="cli-output highlight">
<span class="omni-prefix">[OMNI: Signal Distilled. Dropped 2,784 noisy lines.]</span>
error[E0308]: mismatched types
	--> src/main.rs:12:30
	 |
12 |     let result: String = 100i32;
	 |                          ^^^^^^ expected `String`, found `i32`
error: could not compile `omni` (bin "omni") due to 1 previous error
							</div>
						</div>
					</div>
				</div>

				<div class="diff-visual reveal" style="text-align: center; margin-top: 64px;">
					 <img src="/media/omni-diff.png" alt="OMNI Diff Comparison" style="width: 100%; max-width: 900px; border-radius: 16px; border: 1px solid var(--border-subtle); box-shadow: 0 20px 40px rgba(0,0,0,0.6);">
				</div>
			</div>
		</section>

		<!-- REAL-WORLD USE CASES -->
		<section id="use-cases" class="section">
			<div class="container container-md">
				<header class="section-header reveal">
					<h2 class="section-title">Real-World <span class="text-gradient">Use Cases</span></h2>
					<p class="section-subtitle">How top engineering teams use OMNI to scale multi-agent architectures and fix daily frustrations.</p>
				</header>

				<div class="comparison-bento reveal">
					<div class="bento-card card-danger">
						<div class="card-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg></div>
						<h3>The "Infinite Loop of Death"</h3>
						<p><strong>Scenario:</strong> AI gets distracted by 20,000 lines of <code>npm install</code> warnings, missing the actual build error at the end.</p>
						<p><strong>OMNI's Fix:</strong> Mutes all peer dependency warnings and surfaces only the exact <code>Build Error</code> with its stack trace. AI fixes the code instantly.</p>
					</div>
					<div class="bento-card card-success">
						<div class="card-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></div>
						<h3>The "Silent Hallucination"</h3>
						<p><strong>Scenario:</strong> AI reads a 3,000-line file via <code>cat</code>, struggles to keep it in working memory, and starts hallucinating.</p>
						<p><strong>OMNI's Fix:</strong> Replaces raw <code>cat</code> with a Structured Outline. Shows only imports, public APIs, and risk markers—reducing output by 80%.</p>
					</div>
					<div class="bento-card card-success span-2">
						<div class="card-icon"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></div>
						<h3>Multi-Agent Collaboration</h3>
						<p><strong>Scenario:</strong> Cursor IDE and Claude CLI run simultaneously, executing redundant commands and wasting tokens.</p>
						<p><strong>OMNI's Fix:</strong> Acts as a shared memory layer. Cursor and Claude share the same filtered memory streams, active errors, and environment.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- BENTO FEATURES -->
		<section id="features" class="section">
			<div class="container">
				<header class="section-header reveal">
					<h2 class="section-title">The <span class="text-gradient">Breakthrough</span> Context Layer</h2>
					<p class="section-subtitle">A native, invisible utility belt explicitly built for the Agentic AI era.</p>
				</header>

				<div class="bento-grid reveal">	
					<!-- Distill Monitor -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg></div>
							<h3>Real-Time ROI & Token Analytics</h3>
							<p>Run <code>omni stats</code> to track lifetime token savings, optimization percentages, and financial ROI across all your workspaces in beautiful terminal charts.</p>
						</div>
					   <div class="bento-visual align-bottom">
							 <img  src="/media/omni-stats.png" alt="Visual Impact Analytics" style="width: 100%; border-radius: 8px; border: 1px solid var(--border-subtle);">
						</div>
					</div>

					<!-- Session Intelligence -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></div>
							<h3>Persistent Session Memory</h3>
							<p>OMNI tracks hot files and active errors across your workspace. It natively understands what the AI already knows, injecting contextual continuity into every tool invocation.</p>
						</div>
						<div class="bento-visual align-bottom">
							 <img src="/media/omni-session-status.png" alt="Session Tracking" style="width: 100%; border-radius: 8px; border: 1px solid var(--border-subtle);">
						</div>
					</div>

					<!-- RewindStore -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10"/><path d="m9 12 2 2 4-4"/></svg></div>
							<h3>RewindStore: 100% Context Safety</h3>
							<p>Worried OMNI filtered something important? Our built-in <strong>RewindStore</strong> saves raw output locally via SHA-256. If the AI needs the full log, it automatically retrieves it via MCP.</p>
						</div>
						<div class="bento-visual align-bottom">
							 <img src="/media/omni-rewind-list.png" alt="RewindStore" style="width: 100%; border-radius: 8px; border: 1px solid var(--border-subtle);">
						</div>
					</div>

					<!-- Omni Learn -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v8"/><path d="m16.2 3.8-4.2 4.2"/><path d="M21 12h-8"/><path d="m16.2 20.2-4.2-4.2"/><path d="M12 22v-8"/><path d="m7.8 20.2 4.2-4.2"/><path d="M3 12h8"/><path d="m7.8 3.8 4.2 4.2"/></svg></div>
							<h3>Machine Learning Noise Discovery</h3>
							<p>The <code>omni learn</code> engine automatically analyzes your terminal history to identify repetitive noise patterns, instantly generating optimized semantic TOML filters for your exact tech stack.</p>
						</div>
						<div class="bento-visual align-bottom">
							 <img src="/media/omni-learn-status.png" alt="Omni Learn Discovery" style="width: 100%; border-radius: 8px; border: 1px solid var(--border-subtle);">
						</div>
					</div>

					<!-- Omni Doctor -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg></div>
							<h3>Self-Healing Diagnostic Tools</h3>
							<p>Never worry about broken integrations. The proactive <code>omni doctor</code> utility scans your hooks, MCP configs, and filter performance, offering instant 1-click <code>--fix</code> solutions.</p>
						</div>
						<div class="bento-visual align-bottom">
							 <img src="/media/omni-doctor.png" alt="Omni Doctor Diagnostics" style="width: 100%; border-radius: 8px; border: 1px solid var(--border-subtle);">
						</div>
					</div>

					<!-- Universal Support -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="2.18" ry="2.18"/><line x1="7" y1="2" x2="7" y2="22"/><line x1="17" y1="2" x2="17" y2="22"/><line x1="2" y1="12" x2="22" y2="12"/><line x1="2" y1="7" x2="22" y2="7"/><line x1="2" y1="17" x2="22" y2="17"/></svg></div>
							<h3>Native Integration for 15+ AI Agents</h3>
							<p>Powered by a <strong>Rust Core</strong> for sub-millisecond execution. OMNI seamlessly integrates with Claude Code, Cursor, Cline, Pi, Copilot, Antigravity, and OpenClaw while exposing 12 dynamic MCP Tools.</p>
						</div>
						<div class="bento-visual align-bottom" style="opacity: 0.4;">
							<div class="mock-ui">
								<div class="ui-line"><span class="success">●</span> Supported Agents: 15</div>
								<div class="ui-line"><span class="success">●</span> Tool Filters: 30+ Tools</div>
								<div class="ui-line"><span class="success">●</span> MCP Tools Exposed: 12</div>
							</div>
						</div>
					</div>

					<!-- Autonomous Loops -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21.5 2v6h-6M21.34 15.57a10 10 0 1 1-.92-5.27l-3.46-1"/></svg></div>
							<h3>Production-Grade Autonomous Loops</h3>
							<p>Scale multi-agent tasks cleanly with the Maker-Checker verification pattern. OMNI enforces predictive token constraints (via <code>OMNI_LOOP_BUDGET</code>) and natively orchestrates complex iterative loops.</p>
						</div>
						<div class="bento-visual align-bottom" style="opacity: 0.4;">
							<div class="mock-ui">
								<div class="ui-line"><span class="success">●</span> <span class="text-gradient">OMNI_LOOP_BUDGET</span></div>
								<div class="ui-line"><span class="success">●</span> <span class="text-gradient">OMNI_LOOP_GOAL</span></div>
								<div class="ui-line"><span class="success">●</span> Maker-Checker Validated</div>
							</div>
						</div>
					</div>

					<!-- Engrams -->
					<div class="bento-item">
						<div class="bento-content">
							<div class="icon-wrap"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg></div>
							<h3>Engrams: Cure AI Context Amnesia</h3>
							<p>OMNI automatically detects when a milestone subtask is completed and generates highly compressed state snapshots. Your agent will never suffer from "context amnesia" during long coding sessions.</p>
						</div>
						<div class="bento-visual align-bottom" style="opacity: 0.4;">
							<div class="mock-ui">
								<div class="ui-line"><span class="success">✓</span> Fix: Compiler Error Resolved</div>
								<div class="ui-line"><span class="success">✓</span> Commit: Created schema</div>
								<div class="ui-line"><span class="success">✓</span> State Snapshot Saved</div>
							</div>
						</div>
					</div>
					
				</div>
			</div>
		</section>

		<!-- ARCHITECTURE PIPELINE -->
		<section id="how-it-works" class="section">
			<div class="container">
					<header class="section-header reveal">
						<h2 class="section-title">Command-First <span class="text-gradient">Architecture</span></h2>
						<p class="section-subtitle">The world's first context-aware terminal signal pipeline. Built for the Agentic era.</p>
					</header>

					<div class="pipeline-diagram reveal">
						 <!-- Input -->
						 <div class="pipe-node hover-elevate">
							 <div class="icon-wrap-small"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--text-primary)" stroke-width="2"><polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/></svg></div>
							 <div>
								 <div class="node-title">Command Execution</div>
								 <div class="node-desc">Any terminal command gets intercepted</div>
							 </div>
						 </div>

						 <svg class="pipe-arrow anim-flow" viewBox="0 0 24 24"><line x1="12" y1="2" x2="12" y2="22"/><polyline points="5 15 12 22 19 15"/></svg>

						 <!-- Magic Pipe Detection V2 -->
						 <div class="pipe-node hover-elevate">
							 <div class="icon-wrap-small"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--text-primary)" stroke-width="2"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg></div>
							 <div>
								 <div class="node-title">Magic Pipe Detection V2</div>
								 <div class="node-desc">Process tree & fd context analysis. 99.4% accurate.</div>
							 </div>
						 </div>

						 <svg class="pipe-arrow anim-flow" viewBox="0 0 24 24"><line x1="12" y1="2" x2="12" y2="22"/><polyline points="5 15 12 22 19 15"/></svg>

						 <!-- Command-First Engine -->
						 <div class="pipe-node engine glow-border hover-elevate">
							 <div class="engine-header">
								 <div>
									 <div class="node-title text-gradient">Command-First Engine</div>
									 <div class="node-sub">Domain-Aware Semantic Distillation Pipeline</div>
								 </div>
							 </div>

							 <div class="engine-steps">
								 <div class="step-box">Command</div>
								 <svg class="step-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
								 <div class="step-box">Heuristic Router</div>
								 <svg class="step-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
								 <div class="step-box highlight">Token Scorer</div>
								 <svg class="step-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
								 <div class="step-box">Soft Distiller</div>
								 <svg class="step-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
								 <div class="step-box">ROI Metrics</div>
							 </div>
						 </div>

						 <svg class="pipe-arrow anim-flow" viewBox="0 0 24 24"><line x1="12" y1="2" x2="12" y2="22"/><polyline points="5 15 12 22 19 15"/></svg>

						 <!-- Output Split -->
						 <div class="pipe-node split">
							 <div class="node-split-item output-signal hover-elevate">
								 <div class="icon-wrap-small green-icon"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/></svg></div>
								 <div>
									 <span class="label">Pure Semantic Signal</span>
									 <span class="dest">To AI Agent / LLM</span>
								 </div>
							 </div>
							 <div class="node-split-item output-noise hover-elevate">
								 <div class="icon-wrap-small muted-icon"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg></div>
								 <div>
									 <span class="label">Raw Terminal Noise</span>
									 <span class="dest">RewindStore + Auto Learn</span>
								 </div>
							 </div>
						 </div>
					</div>
			</div>
		</section>
	</main>
    
    <script>
        // Hero canvas animation (from script.js)
        const canvas = document.getElementById('hero-canvas') as HTMLCanvasElement;
        if (canvas) {
            const ctx = canvas.getContext('2d');
            let particles: Array<{x: number, y: number, radius: number, vx: number, vy: number, alpha: number}> = [];
            
            function resize() {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            }
            window.addEventListener('resize', resize);
            resize();

            for (let i = 0; i < 50; i++) {
                particles.push({
                    x: Math.random() * canvas.width,
                    y: Math.random() * canvas.height,
                    radius: Math.random() * 2,
                    vx: (Math.random() - 0.5) * 0.5,
                    vy: (Math.random() - 0.5) * 0.5,
                    alpha: Math.random() * 0.5
                });
            }

            function draw() {
                if (!ctx) return;
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                ctx.fillStyle = '#a1a1aa';
                particles.forEach(p => {
                    p.x += p.vx;
                    p.y += p.vy;
                    if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
                    if (p.y < 0 || p.y > canvas.height) p.vy *= -1;
                    
                    ctx.globalAlpha = p.alpha;
                    ctx.beginPath();
                    ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
                    ctx.fill();
                });
                requestAnimationFrame(draw);
            }
            draw();
        }

        // Copy button logic
        document.querySelectorAll('.copy-btn, .text-link').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const target = e.currentTarget as HTMLElement;
                const text = target.getAttribute('data-copy');
                if (text) {
                    navigator.clipboard.writeText(text);
                    const original = target.innerHTML;
                    if (target.classList.contains('copy-btn')) {
                        target.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent-green)" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>';
                        setTimeout(() => target.innerHTML = original, 2000);
                    } else {
                        target.style.color = 'var(--accent-green)';
                        target.innerText = 'Copied!';
                        setTimeout(() => {
                            target.style.color = '';
                            target.innerText = original;
                        }, 2000);
                    }
                }
            });
        });

        // Dynamic Version Sync (Client-side)
        async function syncVersion() {
            const getLabel = (v) => {
                if (v.includes('-rc')) return 'Release Candidate';
                if (v.includes('-alpha')) return 'Alpha Release';
                if (v.includes('-beta')) return 'Beta Release';
                return 'Stable';
            };
            
            try {
                const res = await fetch('https://api.github.com/repos/fajarhide/omni/releases/latest');
                if (res.ok) {
                    const data = await res.json();
                    const versionEl = document.getElementById('omni-version');
                    const statusEl = document.getElementById('omni-status');
                    
                    if (versionEl && data.tag_name !== versionEl.innerText) {
                        versionEl.innerText = data.tag_name;
                        if (statusEl) {
                            const newLabel = getLabel(data.tag_name);
                            statusEl.innerText = newLabel;
                            statusEl.setAttribute('data-status', newLabel.toLowerCase().replace(' ', '-'));
                        }
                    }
                }
            } catch (e) {}
        }
        syncVersion();
    </script>
</Layout>
